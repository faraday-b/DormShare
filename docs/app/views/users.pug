extends layout

block content
  .container
    h1 User List

    table
      thead
        tr
          th Username
          th Email
          th Points
          th Action

      tbody
        each user in users
          tr
            td= user.username
            td= user.email
            td= user.points
            td
              a.btn(href=`/users/${user.id}`) View Profile